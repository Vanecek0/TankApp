import { SET_USER_NAME, SET_USER_AGE } from "../actions/lastTankHistoryAction"

const initialState = {
    name: "",
    age: 0,
}

function reducer(state=initialState, action:any) {
    switch(action.type) {

    }
}